import Block from "../../Block";
import { CubeAll } from "../CubeAll";
import terrain_texture from "../terrain_texture.json"


export {WhiteGrazedTerracotta, OrangeGrazedTerracotta, MagentaGrazedTerracotta, LightBlueGrazedTerracotta, YellowGrazedTerracotta,
LimeGrazedTerracotta, PinkGrazedTerracotta, GrayGrazedTerracotta, LightGrayGrazedTerracotta,CyanGrazedTerracotta,PurpleGrazedTerracotta,
BlueGrazedTerracotta, BrownGrazedTerracotta, RedGrazedTerracotta, BlackGrazedTerracotta, GreenGrazedTerracotta}

class WhiteGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.white_glazed_terracotta.textures));
    }
}
class OrangeGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.orange_glazed_terracotta.textures));
    }
}
class MagentaGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.magenta_glazed_terracotta.textures));
    }
}
class LightBlueGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.light_blue_glazed_terracotta.textures));
    }
}
class YellowGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.yellow_glazed_terracotta.textures));
    }
}
class LimeGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.lime_glazed_terracotta.textures));
    }
}
class PinkGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.pink_glazed_terracotta.textures));
    }
}
class GrayGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.gray_glazed_terracotta.textures));
    }
}
class LightGrayGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.silver_glazed_terracotta.textures));
    }
}
class CyanGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.cyan_glazed_terracotta.textures));
    }
}
class PurpleGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.purple_glazed_terracotta.textures));
    }
}
class BlueGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.blue_glazed_terracotta.textures));
    }
}
class BrownGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.brown_glazed_terracotta.textures));
    }
}
class GreenGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.green_glazed_terracotta.textures));
    }
}
class RedGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.red_glazed_terracotta.textures));
    }
}
class BlackGrazedTerracotta extends CubeAll{
    constructor(){
        super(Block.getUVMap(terrain_texture.texture_data.black_glazed_terracotta.textures));
    }
}